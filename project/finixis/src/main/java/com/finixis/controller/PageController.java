package com.finixis.controller;

/**
 * Marker interface for page controllers. The applyRole method has been removed —
 * this MVP has no role-based restrictions; every action is available to everyone.
 */
public interface PageController {
    // No role enforcement in this MVP — all actions are always available.
}
