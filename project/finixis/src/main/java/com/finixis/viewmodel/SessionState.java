package com.finixis.viewmodel;

/**
 * Kept for backward compatibility — not used in this MVP.
 * No role-based session exists; the app launches directly to Home with no auth.
 */
public class SessionState {
    // No-op placeholder — role switcher and auth have been removed from the MVP.
}
